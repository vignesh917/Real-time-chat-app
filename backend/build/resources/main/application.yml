spring:
  application:
    name: realtime-chat-backend
  profiles:
    active: ${APP_PROFILE:dev}
  jpa:
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC

server:
  port: 8080
